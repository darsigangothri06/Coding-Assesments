// Given 2 integers which are hours and minutes.
// Convert the time into minutes.

#include<stdio.h>
int main(){
    int a,b;
    scanf("%d%d", &a, &b);
    print("%d", a*60 + b);
    return 0;
}

// Time Complexity - O(1)
// Space Complexity - O(1)

// Test cases are not passed!